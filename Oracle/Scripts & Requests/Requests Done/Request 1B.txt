-- 15/03/2024 --

/*
20240315
owner table column data type data null
VLPDBA SDEDBA REF_CUSTOMER_MISC_INFO OTHER_EDUCATION_SCHL_DESC VARCHAR2(255 CHAR) y
*/

DECLARE
NFOUND NUMBER;
BEGIN
    SELECT COUNT(*) INTO NFOUND FROM ALL_TAB_COLUMNS
    WHERE TABLE_NAME = 'REF_CUSTOMER_MISC_INFO'
    AND OWNER = 'SDEDBA'
    AND COLUMN_NAME = 'OTHER_EDUCATION_SCHL_DESC';
    IF NFOUND = 0 THEN
    EXECUTE IMMEDIATE 'ALTER TABLE SDEDBA.REF_CUSTOMER_MISC_INFO ADD OTHER_EDUCATION_SCHL_DESC VARCHAR2(255 CHAR)';
    END IF;
END;
/

DECLARE
NFOUND NUMBER;
BEGIN
    SELECT COUNT(*) INTO NFOUND FROM ALL_TAB_COLUMNS
    WHERE TABLE_NAME = 'REF_CUSTOMER_MISC_INFO'
    AND OWNER = 'VLPDBA'
    AND COLUMN_NAME = 'OTHER_EDUCATION_SCHL_DESC';
    IF NFOUND = 0 THEN
    EXECUTE IMMEDIATE 'ALTER TABLE VLPDBA.REF_CUSTOMER_MISC_INFO ADD OTHER_EDUCATION_SCHL_DESC VARCHAR2(255 CHAR)';
    END IF;
END;
/